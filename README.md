# salah-app
A personal, ad-free prayer times and Qibla direction web app, built to replace a cluttered ad-heavy app. Single-page HTML app that uses the phone's live GPS to fetch daily prayer times (via the AlAdhan API) and calculate live Qibla direction with a phone-compass-driven needle.
